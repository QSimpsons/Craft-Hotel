# Mallorca FPS

FiveM performance panel in het **Mallorca Roleplay** thema.

Download `mallorca-fps.zip`, pak uit in `resources` en start de resource:

```
ensure mallorca-fps
```

Open in-game met `/fps` of `/fpspanel`.
