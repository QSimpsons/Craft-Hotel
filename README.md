# Mallorca FPS

FiveM performance panel in het **Mallorca Roleplay** thema.

Zet de map `mallorca-fps` in `resources` en start de resource:

```
ensure mallorca-fps
```

Open in-game met `/fps` of `/fpspanel`.
